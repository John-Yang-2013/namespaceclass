---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: namespaceclasses.platform.akuity.io
spec:
  group: platform.akuity.io
  names:
    kind: NamespaceClass
    listKind: NamespaceClassList
    plural: namespaceclasses
    singular: namespaceclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NamespaceClass is the Schema for the namespaceclasses API.
          It is a cluster-scoped resource that defines a set of Kubernetes resources
          to be provisioned into any namespace that references this class.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NamespaceClassSpec defines the desired state of NamespaceClass.
            properties:
              resources:
                description: |-
                  Resources is a list of arbitrary Kubernetes resource templates.
                  Each entry must be a complete Kubernetes manifest (apiVersion, kind, metadata, spec).
                  The controller will create these resources inside every namespace that references this class.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
            type: object
          status:
            description: NamespaceClassStatus defines the observed state of NamespaceClass.
            properties:
              observedGeneration:
                description: ObservedGeneration is the last generation of the NamespaceClass
                  that was reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
